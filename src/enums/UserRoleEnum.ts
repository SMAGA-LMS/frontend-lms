enum UserRolesEnum {
  // TODO: please create also enum in the backend
  // reason: to make sure the role is consistent between frontend and backend (instead of using hardcoded string)
  ADMIN = "ADMIN",
  TEACHER = "TEACHER",
  STUDENT = "STUDENT",
}

export default UserRolesEnum;
